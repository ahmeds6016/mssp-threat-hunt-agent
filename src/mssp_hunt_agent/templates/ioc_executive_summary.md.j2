# IOC Sweep — Executive Summary — {{ s.client_name }}

**Hunt Mode:** Indicator-Driven / IOC Sweep
**Execution Mode:** {{ s.execution_mode | upper }}
**Time Range:** {{ s.time_range }}
{% if s.execution_mode == "mock" %}
> **Note:** This report was generated using simulated (mock) data and does not reflect live environment results.
{% endif %}

---

## Objective

{{ s.sweep_objective }}

## IOC Inventory

| Metric | Count |
|--------|-------|
| IOCs Submitted | {{ s.total_iocs_submitted }} |
| Valid | {{ s.valid_iocs }} |
| Invalid | {{ s.invalid_iocs }} |
| Duplicates Removed | {{ s.dedup_removed }} |

**Type Breakdown:**
{% for ioc_type, count in s.type_breakdown.items() %}
- {{ ioc_type }}: {{ count }}
{% endfor %}

## Sweep Results

| Metric | Count |
|--------|-------|
| IOCs Searched | {{ s.sweep_result.total_iocs_searched }} |
| **Hits** | **{{ s.sweep_result.total_hits }}** |
| No Hits | {{ s.sweep_result.total_misses }} |

{% if s.sweep_result.hits %}
### IOCs Found in Environment

| IOC | Type | Hit Count | First Seen | Last Seen | Affected Users | Affected Hosts |
|-----|------|-----------|------------|-----------|----------------|----------------|
{% for hit in s.sweep_result.hits %}
| {{ hit.ioc_value }} | {{ hit.ioc_type }} | {{ hit.hit_count }} | {{ hit.first_seen }} | {{ hit.last_seen }} | {{ hit.affected_users | join(', ') }} | {{ hit.affected_hosts | join(', ') }} |
{% endfor %}
{% endif %}

{% if s.sweep_result.misses %}
### IOCs Not Found

{% for miss in s.sweep_result.misses %}
- {{ miss }}
{% endfor %}
{% endif %}

## Telemetry Readiness

**{{ s.telemetry_readiness }}** — {{ s.telemetry_rationale }}

## Escalation Recommendation

{{ s.escalation_recommendation }}

## Recommended Next Steps

{% for d in s.detection_engineering_followups %}
{{ loop.index }}. {{ d }}
{% endfor %}

## Limitations

{% for gap in s.gaps %}
- {{ gap }}
{% endfor %}
{% if not s.gaps %}
- None identified.
{% endif %}

---
*Generated by mssp-hunt-agent v0.1.0 — {{ s.execution_mode }} mode — IOC Sweep*
